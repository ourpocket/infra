import { WalletProviderModule } from '../../src/wallet-provider/wallet-provider.module';

describe('WalletProviderModule', () => {
  it('should be defined', () => {
    expect(WalletProviderModule).toBeDefined();
  });
});
