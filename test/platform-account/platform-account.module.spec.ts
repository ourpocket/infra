import { PlatformAccountModule } from '../../src/platform-account/platform-account.module';

describe('PlatformAccountModule', () => {
  it('should be defined', () => {
    expect(PlatformAccountModule).toBeDefined();
  });
});
